import { Link } from "react-router-dom";

function Linktocon(){
    return(
        <div>
             Already registered? <Link to="/">Click here</Link> to login
        </div>
    );
}

export default Linktocon;