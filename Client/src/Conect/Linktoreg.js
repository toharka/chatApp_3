import { Link } from "react-router-dom";

function Linktoreg() {
    return (
        <div>
            Not registered? <Link to="../Register">Click here</Link>
        </div>

    );
}

export default Linktoreg;